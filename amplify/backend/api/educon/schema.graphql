type Notes @model @auth(rules: [{allow: public}]) {
  id: ID!
  content: String
}

type Videos @model @auth(rules: [{allow: public}]) {
  id: ID!
  filename: String
  category: String
  URL: AWSURL
}
 